{{#extend "layout"}}
  {{#content "title"}}{{basics.name}}{{/content}}
  {{#content "header"}}
      <h2>{{basics.name}}</h2>
      <h4>{{basics.label}}</h4>
      {{#if basics.profiles.length}}
          <ul class="nav navbar-nav">
            {{#each basics.profiles}}
                <li><a href="{{url}}"><i class="fa fa-{{toLower network}} fa-border"></i>{{username}}</a></li>
            {{/each}}
          </ul>
      {{/if}}
      <ul class="nav navbar-nav navbar-right">
        {{#if basics.website }}
            <li><a href="{{basics.website}}">{{basics.website}}</a></li>
        {{/if}}
        {{#if basics.email }}
            <li><a href="mailto:{{basics.email}}">{{basics.email}}</a></li>
        {{/if}}
        {{#if basics.phone }}
            <li><a href="#">{{basics.phone}}</a></li>
        {{/if}}
      </ul>
  {{/content}}
  {{#content "summary"}}
      <i class="fa fa-user fa-2x fa-border fa-pull-left"></i>
      <h2 class="sectionTitle clearfix">Summary</h2>
      <p class="summary">{{basics.summary}}</p>
  {{/content}}
  {{#content "experience"}}
      <article>
          <header>
              <i class="fa fa-suitcase fa-2x fa-border fa-pull-left"></i>
              <h2 class="sectionTitle clearfix">Experience</h2>
          </header>
          <div class="col-md-1"></div>
          <div class="col-md-11">
            {{#each work}}
                <div class="row">
                  {{#if website}}
                      <h4 class="company pull-left"><a href="{{website}}">{{company}}</a></h4>
                  {{else}}
                      <h4 class="company pull-left">{{company}}</h4>
                  {{/if}}
                    <time class="date">
                      {{#if endDate}}
                        {{#if startDate}}
                            <span class="startDate" datetime="{{startDate}}">{{startDate}}</span>
                            <span class="endDate" datetime="{{endDate}}">- {{endDate}}</span>
                        {{else}}
                            <span class="endDate" datetime="{{endDate}}">{{endDate}}</span>
                        {{/if}}
                      {{else}}
                          <span class="endDate">Present</span>
                      {{/if}}
                    </time>
                    <div class="clearfix"></div>
                  {{#if position}}
                      <h5 class="position">{{position}}</h5>
                  {{/if}}
                </div>
                <div class="row">
                  {{#if summary}}
                      <div class="summary">
                          <p>{{summary}}</p>
                      </div>
                  {{/if}}
                </div>
                <div class="row">
                  {{#if highlights.length}}
                      <ul class="highlights">
                        {{#each highlights}}
                            <li>{{.}}</li>
                        {{/each}}
                      </ul>
                  {{/if}}
                </div>
            {{/each}}
          </div>
      </article>
  {{/content}}
  {{#content "education"}}
      <article>
          <header>
              <i class="fa fa-university fa-2x fa-border fa-pull-left"></i>
              <h2 class="sectionTitle clearfix">Education</h2>
          </header>
          <div class="col-md-1"></div>
          <div class="col-md-11">
            {{#each education}}
                <div class="row">
                  {{#if area}}
                      <h4 class="area pull-left">{{area}} {{studyType}}</h4>
                  {{/if}}
                    <time class="date">
                      {{#if endDate}}
                        {{#if startDate}}
                            <time class="startDate" datetime="{{startDate}}">{{startDate}}</time>
                            <time class="endDate" datetime="{{endDate}}">- {{endDate}}</time>
                        {{else}}
                            <time class="endDate" datetime="{{endDate}}">{{endDate}}</time>
                        {{/if}}
                      {{else}}
                          <span class="endDate">Present</span>
                      {{/if}}
                    </time>
                    <div class="clearfix"></div>
                  {{#if institution}}
                      <h5 class="institution">{{institution}}</h5>
                  {{/if}}
                </div>
                <div class="row">
                  {{#if gpa}}
                      <div class="gpa">GPA: {{gpa}}</div>
                  {{/if}}
                </div>
                <div class="row">
                  {{#if courses.length}}
                      <ul class="courses">
                        {{#each courses}}
                            <li>{{.}}</li>
                        {{/each}}
                      </ul>
                  {{/if}}
                </div>
            {{/each}}
          </div>
          </div>
      </article>
  {{/content}}
  {{#content "skills"}}
      <article>
          <header id="header-skills">
              <i class="fa fa-cog fa-2x fa-border fa-pull-left"></i>
              <h4 class="title text-uppercase clearfix">Skills</h4>
          </header>
          <div class="col-md-1"></div>
          <div class="col-md-11">
            {{#each skills}}
              {{#if name}}
                  <h4 id="skills-name">{{name}}</h4>
              {{/if}}
              {{#if keywords.length}}
                  <ul class="skills-keywords">
                    {{#each keywords}}
                        <li class="skills-keyword">{{.}}</li>
                    {{/each}}
                  </ul>
              {{/if}}
            {{/each}}
          </div>
      </article>
  {{/content}}
  {{#content "interests"}}
      <article>
          <header id="header-interests">
              <i class="fa fa-pencil fa-2x fa-border fa-pull-left"></i>
              <h4 class="title text-uppercase clearfix">Interests</h4>
          </header>
          <div class="col-md-1"></div>
          <div class="col-md-11">
            {{#each interests}}
                <div class="row">
                  {{#if name}}
                      <h4 id="interests-name">{{name}}</h4>
                  {{/if}}
                  {{#if keywords.length}}
                      <ul class="interests-keywords">
                        {{#each keywords}}
                            <li class="interests-keyword">{{.}}</li>
                        {{/each}}
                      </ul>
                  {{/if}}
                </div>
            {{/each}}
          </div>
      </article>
  {{/content}}
  {{#content "footer"}}
  {{/content}}
{{/extend}}
